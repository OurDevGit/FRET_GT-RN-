import React from "react";
import { View } from "react-native";

const FretboardString = ({ isBass }) => (
  <View style={{ flex: 1, backgroundColor: "red" }}>
  </View>
);

export default FretboardString;